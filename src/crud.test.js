const Update = require('./Update')
const delet = require('./Delete')
const add = require('./Create')
const fillTable = require('./Read')

test('should Create', () => {
    console.log(add)
})

test('should update', () => {

    console.log(Update)
})

test('should Delete', () => {
    console.log(delet)
})